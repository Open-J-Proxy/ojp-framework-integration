<?xml version="1.0" encoding="UTF-8"?>
<arquillian xmlns="http://jboss.org/schema/arquillian"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://jboss.org/schema/arquillian
                http://jboss.org/schema/arquillian/arquillian_1_0.xsd">

    <!--
        Embedded GlassFish 7 container configuration for Arquillian integration tests.
        The OmniFaces arquillian-glassfish-server-embedded adapter manages the lifecycle
        of an in-process GlassFish 7 instance.
    -->
    <container qualifier="glassfish-embedded" default="true">
        <configuration>
            <!-- Use a non-default port to avoid conflicts -->
            <property name="bindHttpPort">9090</property>
        </configuration>
    </container>

</arquillian>
